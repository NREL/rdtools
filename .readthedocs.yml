version: 2

sphinx:
  # Path to your Sphinx configuration file.
  configuration: docs/sphinx/source/conf.py

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"

python:
    install:
        - method: pip
          path: .
          extra_requirements:
              - doc
